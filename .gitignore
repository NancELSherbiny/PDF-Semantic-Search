# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Local model cache / vector store data (created at runtime)
.cache/
qdrant_storage/

# Uploaded / sample PDFs — keep the folder + the committed sample, ignore the rest
data/*.pdf
!data/.gitkeep
!data/sample.pdf

# OS noise
.DS_Store
